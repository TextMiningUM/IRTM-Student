# =============================================================================
# IRTM 2026-2027 — Python Dependencies
# =============================================================================
# Tested with Python 3.13 (same as the course JupyterHub cluster).
#
# Install with:  pip install -r requirements.txt
#
# PyTorch CUDA support: De extra-index-url hieronder zorgt ervoor dat
# torch en torchvision automatisch met GPU/CUDA 12.4 support worden geïnstalleerd.
# 

--extra-index-url https://download.pytorch.org/whl/cu124

# --- Core scientific stack ---------------------------------------------------
numpy
pandas
scipy
matplotlib
seaborn
scikit-learn
tqdm

# --- NLP foundations ---------------------------------------------------------
nltk
beautifulsoup4
gensim
spacy
langdetect
transliterate
unidecode

# --- Deep learning / Transformers --------------------------------------------
torch
torchvision
transformers
accelerate
sentence-transformers

# --- HuggingFace ecosystem ---------------------------------------------------
datasets
evaluate

# --- Search & retrieval ------------------------------------------------------
pyserini                  # requires Java JDK 11+ (see LOCAL_SETUP.md)
faiss-cpu
rank_bm25
PyMuPDF                   # imported as "fitz"

# --- NER / sequence labelling ------------------------------------------------
python-crfsuite
sklearn-crfsuite
seqeval
fastcoref

# --- Topic modelling ---------------------------------------------------------
pyLDAvis
bertopic
umap-learn
hdbscan

# --- Evaluation & metrics ----------------------------------------------------
rouge-score
bert-score

# --- Visualisations ----------------------------------------------------------
plotly
wordcloud
svgling

# --- String matching ---------------------------------------------------------
python-Levenshtein

# --- Graphs ------------------------------------------------------------------
networkx

# --- OpenAI (Tutorials 11 & 12 only) ----------------------------------------
openai>=1.40.0
openai-agents
tiktoken
