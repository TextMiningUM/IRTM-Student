# =============================================================================
# IRTM 2025-2026 — Python Dependencies
# =============================================================================
# Tested with Python 3.10 (same as the course JupyterHub cluster).
# Python 3.9–3.11 should also work; Python 3.12+ may have compatibility
# issues with some packages.
#
# Install with:  pip install -r requirements.txt
#
# GPU SUPPORT (NVIDIA only):
#   This installs CPU-only PyTorch by default. For GPU acceleration:
#   1. First uninstall: pip uninstall torch torchvision torchaudio -y
#   2. Then install CUDA version: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   3. Works with all modern NVIDIA GPUs (RTX 30xx, 40xx, 50xx, A100, H100, etc.)
#   4. See LOCAL_SETUP.md section 8 for full instructions
# =============================================================================

# --- Core scientific stack ---------------------------------------------------
numpy
pandas
scipy
matplotlib
seaborn
scikit-learn
tqdm

# --- NLP foundations ---------------------------------------------------------
nltk
beautifulsoup4
gensim
spacy
langdetect
transliterate
unidecode

# --- Deep learning / Transformers --------------------------------------------
torch
transformers
accelerate
sentence-transformers

# --- HuggingFace ecosystem ---------------------------------------------------
datasets
evaluate

# --- Search & retrieval ------------------------------------------------------
pyserini                  # requires Java JDK 11+ (see LOCAL_SETUP.md)
faiss-cpu
rank_bm25
PyMuPDF                   # imported as "fitz"

# --- NER / sequence labelling ------------------------------------------------
python-crfsuite
sklearn-crfsuite
seqeval
fastcoref

# --- Topic modelling ---------------------------------------------------------
pyLDAvis
bertopic
umap-learn
hdbscan

# --- Evaluation & metrics ----------------------------------------------------
rouge-score
bert-score

# --- Visualisations ----------------------------------------------------------
plotly
wordcloud
svgling

# --- String matching ---------------------------------------------------------
python-Levenshtein

# --- Graphs ------------------------------------------------------------------
networkx

# --- OpenAI (Tutorials 11 & 12 only) ----------------------------------------
openai>=1.40.0
openai-agents
tiktoken
