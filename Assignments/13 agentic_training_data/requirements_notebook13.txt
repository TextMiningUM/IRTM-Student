# Notebook 13 — From Text to Agentic Training Data
# Pinned to the versions used to author the notebook; update freely as needed.

# --- Core LLM / API ---
openai>=1.40.0
tiktoken>=0.7.0

# --- Retrieval / NLP ---
spacy>=3.7.0
rank_bm25>=0.2.2
sentence-transformers>=2.7.0

# --- Data & utilities ---
datasets>=2.18.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
networkx>=3.2

# --- Plotting ---
matplotlib>=3.8.0

# --- Optional Tier-2 (real LoRA-SFT + DPO adapters in §9.7b/c, §10.7b/c) ---
# These are heavy. Skip if you only want Tier-1 prompt-only conditions.
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.29.0
bitsandbytes>=0.43.0; platform_system != "Windows"

# --- Progress bars used inside long loops ---
tqdm>=4.66.0
